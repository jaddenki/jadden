---
import BaseHead from "../components/BaseHead.astro";
import { HoverPreviewLink } from "../components/HoverPreviewLink";
import ImageGrid from "../components/ImageGrid.astro";
import InlineAvatar from "../components/InlineAvatar.astro";
import ThemeToggle from "../components/ThemeToggle.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
import "../styles/global.css";

const projects = [
    {
        src: "/img/front/capytitle.webm",
        alt: "Capy",
        aspectRatio: "16/9",
        freezeTime: 0.6,
        playContinuously: true,
    },
    {
        src: "/img/front/capycapy.webm",
        alt: "Capy",
        aspectRatio: "16/9",
        freezeTime: 0.6,
        playContinuously: true,
    },
    {
        src: "/img/front/infrared.webm",
        alt: "Infrared - Personal",
        aspectRatio: "1/1",
        freezeTime: 0.6,
        // playContinuously: true,
    },
    {
        src: "/img/front/hello_world.webm",
        alt: "Hello World - Personal",
        aspectRatio: "1/1",
        freezeTime: 3.5,
    },
    {
        src: "/img/front/fish.webm",
        alt: "Fish - Personal",
        aspectRatio: "1/1",
        freezeTime: 0.8,
    },
    {
        src: "/img/front/loco.webm",
        alt: "Go Loco - Personal",
        aspectRatio: "1/1",
        freezeTime: 3.4,
    },
    // {
    // 	src: '/img/front/capy-text.webm', not spoiling
    // 	alt: 'capy text',
    // 	aspectRatio: '16/9',
    // },
    // {
    // 	src: '/img/front/capy-ui.webm',
    // 	alt: 'capy ui',
    // 	aspectRatio: '16/9',
    // },
    {
        src: "/img/front/respellai.webm",
        alt: "Respell AI",
        aspectRatio: "16/9",
        freezeTime: 0.5,
        // playContinuously: true,
    },
    {
        src: "/img/front/stl.webm",
        alt: "Spread the Love",
        aspectRatio: "16/9",
        freezeTime: 2.7,
    },
    {
        src: "/img/front/indigo.webm",
        alt: "Indigo",
        aspectRatio: "1/1",
        freezeTime: 0.5,
        // playContinuously: true,
    },
    {
        src: "/img/front/anklyosaur.webm",
        alt: "Anklyosaur - Personal",
        aspectRatio: "1/1",
        freezeTime: 3.6,
    },
    {
        src: "/img/front/glass.webm",
        alt: "Glass - Personal",
        aspectRatio: "1/1",
        freezeTime: 3.3,
    },
    {
        src: "/img/front/ridelikebike.webm",
        alt: "Drop It - Personal",
        aspectRatio: "1/1",
        freezeTime: 1.8,
    },
];

interface ProjectListItem {
    name: string;
    href?: string;
    gridIndex?: number[];
    previewSrc?: string;
    previewAlt?: string;
    previewText?: string;
    tag?: string;
}

const projectList: ProjectListItem[] = [
    {
        name: "Capy",
        gridIndex: [0, 1],
        href: "https://x.com/capydotai/status/2031430074940195126",
        previewText: "Launch Video ↗",
        previewAlt: "Link to Twt",
        tag: "Branding Video",
    },
    {
        name: "PixelHackers",
        href: "https://x.com/igarakii/status/2029082607711863007?s=20",
        previewText: "Still working on it...↗",
        tag: "Typeface",
    },
    {
        name: "etea.cafe",
        href: "https://etea.cafe",
        previewText: "Daily Web Game ↗",
        tag: "Game",
    },
    // { name: "etea.cafe", href: "https://etea.cafe", previewSrc: "/img/previews/lowercase.webp", previewAlt: "etea.cafe" },
    {
        name: "Spread the Love",
        gridIndex: [7],
        href: "https://store.steampowered.com/app/3852520/Spread_The_Love/",
        previewText: "Steam ↗",
        tag: "Game",
    },
    {
        name: "Respell AI",
        gridIndex: [6],
        href: "https://respell.ai",
        previewText: "Site ↗",
        previewAlt: "Link to Respell AI",
        tag: "Video",
    },
];

const isVideoUrl = (url: string) => /\.(webm)(\?|$)/i.test(url);
---

<!doctype html>
<html lang="en">
    <head>
        <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
        <link
            rel="preload"
            as={isVideoUrl(projects[0].src) ? "video" : "image"}
            href={projects[0].src}
        />
    </head>
    <body>
        <script is:inline>
            if (sessionStorage.getItem("hasVisited"))
                document.body.classList.add("loading-skip");
        </script>
        <div id="loading-screen">
            <div class="loading-content">
                <div class="loading-pixels" aria-hidden="true">
                    <span class="loading-pixel"></span>
                    <span class="loading-pixel"></span>
                    <span class="loading-pixel"></span>
                </div>
                <p class="loading-text">Let's take a deep breath...</p>
            </div>
        </div>
        <div id="page-ready" aria-live="polite" class="sr-only"></div>
        <main class="homepage-container">
            <div class="content text-reveal">
                <p style="--reveal-order: 0">
                    Hi, I'm <InlineAvatar
                        src="https://avatars.githubusercontent.com/jaddenki"
                        alt="Jadden's GitHub profile picture"
                        width={16}
                        height={16}
                        class="inline-avatar"
                        priority
                    />
                    Jadden Picardal (she/her) <br />Read more <HoverPreviewLink
                        client:visible
                        href="/about"
                        data-astro-prefetch
                        previewText="about me >">about me</HoverPreviewLink
                    >
                </p>
                <!-- span class="email">jadden [at] jam [dot] ms</span> -->
                <p style="--reveal-order: 1">
                    I'm a programmer and designer, studying computer engineering
                    at
                    <HoverPreviewLink
                        client:visible
                        href="https://www.purdue.edu/"
                        target="_blank"
                        previewSrc="/img/previews/purdue.webp"
                        previewAlt="Purdue">Purdue</HoverPreviewLink
                    >
                    <span class="inline-text">(Fall 2026)</span>
                </p>

                <p style="--reveal-order: 2">
                    Currently, I'm making unconventional branding for startups
                    at <InlineAvatar
                        src="https://avatars.githubusercontent.com/lowercaseclub"
                        alt=""
                        width={20}
                        height={20}
                        class="inline-avatar"
                    />
                    <HoverPreviewLink
                        client:visible
                        href="https://lowercase.club/"
                        target="_blank"
                        previewSrc="/img/previews/lowercase.webp"
                        previewAlt="lowercase.club preview"
                        >lowercase</HoverPreviewLink
                    >, focusing on motion + web design
                </p>

                <!-- <p>
                    I'm also the Comms Lead and a Designer
                    at <InlineAvatar
                        src="https://www.google.com/s2/favicons?domain=purduehackers.com&sz=64"
                        alt=""
                        width={20}
                        height={20}
                        class="inline-avatar inline-avatar-invert-dark"
                    />
                    <HoverPreviewLink
                        client:visible
                        href="https://purduehackers.com/"
                        target="_blank"
                        previewSrc="/img/previews/ph.webp"
                        previewAlt="purduehackers.com preview"
                    >Purdue Hackers</HoverPreviewLink>, a the coolest community of makers on campus
                </p> -->

                <p style="--reveal-order: 3">
                    Links... <HoverPreviewLink
                        client:visible
                        href="https://github.com/jaddenki"
                        target="_blank"
                        previewText="@jaddenki ↗">GitHub</HoverPreviewLink
                    >, <HoverPreviewLink
                        client:visible
                        href="https://www.linkedin.com/in/jadden-picardal"
                        target="_blank"
                        previewText="jadden-picardal ↗"
                        >LinkedIn</HoverPreviewLink
                    >, <HoverPreviewLink
                        client:visible
                        href="https://www.are.na/jadden-picardal/jadden-picardal-s0qvounprtc"
                        target="_blank"
                        previewText="Jadden Picardal ↗">Are.na</HoverPreviewLink
                    >, <HoverPreviewLink
                        client:visible
                        href="https://x.com/igarakii"
                        target="_blank"
                        previewText="@igarakii ↗">Twitter</HoverPreviewLink
                    >
                </p>

                <p style="--reveal-order: 4">Some things I've worked on...</p>

                <ul class="project-list" style="--reveal-order: 5">
                    {
                        projectList.map((p) => {
                            const hasPreview = p.previewSrc || p.previewText;
                            return (
                                <li data-grid-index={p.gridIndex?.join(",")}>
                                    <div class="project-list-row">
                                        <span class="project-list-main">
                                            {hasPreview && p.href ? (
                                                <HoverPreviewLink
                                                    client:visible
                                                    href={p.href}
                                                    target={
                                                        p.href.startsWith("/")
                                                            ? undefined
                                                            : "_blank"
                                                    }
                                                    previewSrc={p.previewSrc}
                                                    previewAlt={p.previewAlt}
                                                    previewText={p.previewText}
                                                >
                                                    {p.name}
                                                </HoverPreviewLink>
                                            ) : p.href ? (
                                                <a href={p.href}>{p.name}</a>
                                            ) : (
                                                <span>{p.name}</span>
                                            )}
                                        </span>
                                        {p.tag && (
                                            <span class="project-tag">
                                                {p.tag}
                                            </span>
                                        )}
                                    </div>
                                </li>
                            );
                        })
                    }
                </ul>
                <p style="--reveal-order: 6">
                    And more! Currently populating this space...
                </p>
            </div>

            <ImageGrid items={projects} eager />
        </main>

        <footer>
            <ThemeToggle />
            <nav class="webring-nav" aria-label="Purdue Hackers webring">
                <a
                    href="https://ring.purduehackers.com/previous?host=https://www.jaddens.page"
                    class="webring-link"
                    aria-label="Previous site in Purdue Hackers webring"
                >
                    &lt;
                </a>
                <a
                    href="https://ring.purduehackers.com/"
                    class="webring-link webring-logo"
                    aria-label="Purdue Hackers webring home"
                >
                    Purdue Hackers
                </a>
                <a
                    href="https://ring.purduehackers.com/next?host=https://www.jaddens.page"
                    class="webring-link"
                    aria-label="Next site in Purdue Hackers webring"
                >
                    &gt;
                </a>
            </nav>
        </footer>
        <script>
            const hasVisited = sessionStorage.getItem("hasVisited");
            const loadingScreen = document.getElementById("loading-screen");
            if (hasVisited && loadingScreen) {
                loadingScreen.remove();
            } else {
                // only for first vidit
                window.addEventListener("load", () => {
                    if (loadingScreen) {
                        sessionStorage.setItem("hasVisited", "true");
                        loadingScreen.classList.add("fade-out");
                        loadingScreen.addEventListener(
                            "transitionend",
                            () => {
                                loadingScreen.remove();
                                const liveRegion = document.getElementById("page-ready");
                                if (liveRegion) liveRegion.textContent = "Page loaded";
                            },
                            {
                                once: true,
                            },
                        );
                        setTimeout(() => {
                            if (loadingScreen.isConnected)
                                loadingScreen.remove();
                        }, 700);
                    }
                });
            }
        </script>
        <script>
            const listItems = document.querySelectorAll<HTMLElement>(
                ".project-list li[data-grid-index]",
            );
            const gridTiles =
                document.querySelectorAll<HTMLElement>(".gallery-grid > *");
            const gallery = document.querySelector(".gallery-grid");
            let leaveTimer: ReturnType<typeof setTimeout> | null = null;

            const dispatchToTileVideos = (
                tile: HTMLElement,
                type: "mouseenter" | "mouseleave",
            ) => {
                const videos = tile.querySelectorAll<HTMLVideoElement>(
                    "video.gallery-image",
                );
                for (const video of videos) {
                    video.dispatchEvent(new Event(type, { bubbles: false }));
                }
            };

            const clearGrid = () => {
                gallery?.classList.remove("grid-dimmed");
                for (const tile of gridTiles) {
                    tile.classList.remove("grid-highlight");
                    // Let videos return to their idle / freeze frame state
                    dispatchToTileVideos(tile, "mouseleave");
                }
            };

            for (const li of listItems) {
                const trigger = li.querySelector<HTMLAnchorElement>("a");
                if (!trigger) continue;

                trigger.addEventListener("mouseenter", () => {
                    if (leaveTimer) {
                        clearTimeout(leaveTimer);
                        leaveTimer = null;
                    }
                    const indicesArray = li.dataset
                        .gridIndex!.split(",")
                        .map(Number);
                    const indices = new Set(indicesArray);
                    gallery?.classList.add("grid-dimmed");
                    // Smooth-scroll the first matching tile into view
                    const firstIndex = Math.min(...indicesArray);
                    const targetTile = gridTiles[firstIndex];
                    if (targetTile) {
                        const prefersReduced = window.matchMedia(
                            "(prefers-reduced-motion: reduce)"
                        ).matches;
                        targetTile.scrollIntoView({
                            behavior: prefersReduced ? "instant" : "smooth",
                            block: "center",
                            inline: "nearest",
                        });
                    }
                    for (let i = 0; i < gridTiles.length; i++) {
                        const tile = gridTiles[i];
                        const shouldHighlight = indices.has(i);
                        tile.classList.toggle(
                            "grid-highlight",
                            shouldHighlight,
                        );
                        if (shouldHighlight) {
                            // Trigger the same behavior as hovering the video tile directly
                            dispatchToTileVideos(tile, "mouseenter");
                        } else {
                            dispatchToTileVideos(tile, "mouseleave");
                        }
                    }
                });
                trigger.addEventListener("mouseleave", () => {
                    if (leaveTimer) clearTimeout(leaveTimer);
                    leaveTimer = setTimeout(clearGrid, 75);
                });
            }
        </script>
    </body>
</html>
